services:
  metar-proxy:
    build: .
    container_name: metar-proxy
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      PORT: "3001"
      # 建議改成你實際的 AMPS 網域，例如 https://steven74423.github.io，避免被其他網站盜用
      ALLOWED_ORIGIN: "*"
