services:
  metar-proxy:
    build: .
    container_name: metar-proxy
    restart: unless-stopped
    ports:
      - "3011:3001"
    environment:
      PORT: "3001"
      ALLOWED_ORIGIN: "https://stevenkate.synology.me"
